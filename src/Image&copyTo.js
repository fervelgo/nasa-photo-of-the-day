import React, { useState, useEffect } from "react";
import ReactDOM from "react-dom";

PhotoCont = (props) => {
const 
return (
    <div className="PhotoCont">
        Aquí va la foto:
        <img src = `${res.data.title}` alt= 'Image from space'>
    </div>
)

}


export default PhotoCont;